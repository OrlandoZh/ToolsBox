# Behavior Specification (Rewrite Baseline)

Use this file to define *what* the plugin should do, without referencing prior source code.

## Product Scope
- Plugin name:
- Target Zotero versions:
- Supported platforms:

## Feature List
1. Command registration
   - Trigger:
   - Expected behavior:
   - Error behavior:
2. UI injection
   - Trigger:
   - Expected behavior:
   - Accessibility notes:
3. Preference handling
   - Inputs:
   - Persistence behavior:
   - Validation:

## Non-Functional Requirements
- Startup latency target:
- Error logging policy:
- Internationalization requirements:

## Acceptance Criteria (Black-Box)
- [ ] On startup, expected commands appear in UI.
- [ ] On command invoke, action result matches spec.
- [ ] On shutdown, listeners/timers are cleaned up.

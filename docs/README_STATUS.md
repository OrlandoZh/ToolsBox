# ToolsBox README Status

**更新时间**: 2026-05-19
**权威入口**: [CURRENT_BACKLOG.md](./CURRENT_BACKLOG.md)

README 和框架镜像只同步 `CURRENT_BACKLOG.md` 的 marker block，不维护独立完成度。

## 当前 README 应表达

- 当前主线为 `STYLE-CUSTOM-EXTERNAL-API-OPT-IN-WORKFLOW-WAVE-001`。
- 发布已冻结，远端分发未完成。
- Style-like prototype 默认关闭，未实现能力保持 planned/default-off。
- Backlinks、Merge Annotations 与 Attachment Version 已作为 default-off prototype 形成本地/真机证据。
- Paper Matrix Enhanced 已作为 default-off local field/filter/sort preview 收口。
- Style Editor 已作为 default-off local CSS standalone window prototype 收口。
- TLDR 已作为 default-off local Item Pane no-network preview 收口；不做真实 AI summary、真实 provider 或 endpoint 调用。
- Custom External API 当前推进 default-off opt-in endpoint invocation preview；显式启用且 endpoint 非空后才注册 Item Pane section，手动 Run 发送 allowlisted metadata POST，只渲染 response preview，不写 Zotero 数据。
- release preflight artifacts 已生成，但 remote verification pending。

## 禁止回写旧口径

- 不写 `313 tests 100%` 或功能百分比。
- 不写 `zotero-style` 全量复刻完成。
- 不把 release matrix `attention` 误写成远端发布完成。
